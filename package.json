{
  "name": "spoolman-duetwebcontrol",
  "version": "0.5.0",
  "private": true,
  "description": "Spoolman integration for DuetWebControl with server-side tracking",
  "workspaces": [
    "server"
  ],
  "scripts": {
    "lint": "npm run lint --workspace server",
    "test": "npm run test --workspace server",
    "build:server": "npm run build --workspace server",
    "build:plugin": "node ./scripts/build-plugin-zip.mjs",
    "build": "npm run build:server && npm run build:plugin"
  }
}
